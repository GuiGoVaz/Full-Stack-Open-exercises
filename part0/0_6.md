sequenceDiagram
participant Browser
participant Server
Note right of Browser: After clicking the button to save the note, the JavaScript code fetched previously adds the note to the list and rerenders it
Note right of Browser: Then the code makes a POST request to /new_note_spa , with the note in JSON format as payload
Browser->>+Server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
Server-->>-Browser: Status Code: 201 created
